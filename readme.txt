1- in order to execute the file, use the command below:
python main.py

2- in the .ipynb notebook available in the current directory, you can find the procedure taken to solve the problem.

3- the folder result is empty, but please don't delete it. when the execution of file is finished, the reports will be saved in the result folder.

4- if you run the given command, it will start to train for only 3 epochs in order to get a fast result. it is recommended to increase this number if you want to get a better result.

5- there is a folder named result-50, in which you can see the result of the network after running for 100 epochs

6- I don't know why layers I used for augmentation are generating so many warnings ;) sorry for that.
